services:
  app:
    image: busybox:1.36
    command:
      - sh
      - -c
      - |
        mkdir -p /data
        echo "Journey docker demo ready" >/data/state.txt
        echo "Journey docker demo ready"
        sleep 300
    volumes:
      - app_data:/data

volumes:
  app_data: {}
